<div class="container my-5">
  <div class="row justify-content-center">
   <div class="sweet-index">
  <%= form_with model: @sweet, url: admin_sweet_path do |f| %>
    <h2><%= @sweet.name %></h2>
    <br>
  <div class="sweet-item">
      <div>
        <dl>
          <dt>
            <%= image_tag @sweet.get_image, class: "sweet-img" %>
          </dt>
          <br>
          <dd>
            <%= @sweet.name %>
          </dd>
          <dd>
            <%= @sweet.description %>
          </dd>
          <br><br>
          <dd>
            <%= link_to "編集する", edit_admin_sweet_path(@sweet), class: "btn btn-outline-danger mx-auto my-1" %>
          </dd>
        </dl>
      </div>
  </div>
  <% end %>
   </div>
  </div>
</div>

<!-- monthをmigrateに追加して、同じ内容を繰り返さないように、seeds.rbを編集する。-->