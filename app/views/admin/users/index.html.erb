<div class="container my-5"> <!-- text-centerとは、Bootstrapを使って中央寄せをする指定する方法。詳しくは、https://qiita.com/Rock22/items/e4e89f15c29e1415977dを参照。-->
    <div class="row">
        <h2>会員一覧</h2>
    </div>
        
        <div class="row mt-3">
            <div class="offset-sm-1 col-sm-10">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>会員ID</th>
                        <th>ユーザーネーム</th>
                        <th>メールアドレス</th>
                    </tr>
                </thead>
                <tbody>
                  <% @users.each do |user| %>
                    <tr>
                      <td><%= user.id %></td>
                      <td>
                          <%= user.name %>
                      </td>
                      <td><%= user.email %></td>
                      <td>
                          <%= button_to "削除", admin_user_path(user.id), method: :delete, data: {confirm: "本当に削除してもよろしいですか？"} %>
                      </td>
                    </tr>
                   <% end %>
                </tbody>
            </table>
           </div>
       </div>
       <div class="container">
           <div class="row">
               <div class="col-md-4 offset-md-4 py-2 mt-5">
                   <%= paginate @users %>
               </div>
           </div>
       </div>
</div>