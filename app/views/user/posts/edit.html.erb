<div class="container my-5">
    <div class="post-title">
        <h2>投稿編集</h2>
        <br><br>
        <div class="post-item">
            <div>
                <dl>
                    <%= form_with model: @post, url: post_path do |f|%> <!--　ここのURLは必ず指定する！ -->
                    <dt>
                      <div class="item-new-form">
                         <%= f.file_field :image, placeholder:"プロフィール画像", accept: "image/*" %> <!-- file_fieldとは、ファイルの投稿をするためのフィールドであり、画像ファイル以外に音声ファイルや動画ファイルも投稿できてしまうので、'accept: "image/*"を使い、画像ファイルしか投稿できなくする。-->
                      </div>
                    </dt>
                    <dd>
                        <%= f.text_field :title, autofocus: true, placeholder:"タイトル", class: "form-control" %>
                    </dd>
                    <br><br>
                    <dd>
                        <%= f.text_area :content, placeholder:"本文", class: "form-control" %>
                        <%= f.submit "変更を保存", class: "btn btn-primary text-right" %>
                    </dd>
                    <% end %>
                </dl>
            </div>
        </div>
    </div>
</div>